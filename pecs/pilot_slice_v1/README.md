# Pilot_Slice_V1_Contract_Pack_v1.0

P0 contracts and fixtures only. No application, Streamlit, SQLite implementation, transformation engine, URL/JSON ingestion or LLM code is included.

Scope: one local CSV or one selected XLSX sheet; manual Brief and TransformationSpec; immutable Approval facts; 12 deterministic operations; CSV/XLSX export; minimal deterministic QA.

Instance `schema_version` is `1.1`; package release is `1.0`. See `VALIDATION_REPORT.md` and `OPEN_DECISIONS.md`.
